# CLAUDE.md — 贪吃蛇屏幕保护程序（snake-screensaver）

> 本文档是项目的需求规格与开发规范，是后续所有迭代开发的唯一依据。修改需求时必须同步更新本文档。

## 1. 项目概述

一个 Windows 屏幕保护程序（.scr）：AI 自动操控的贪吃蛇在屏幕网格中移动、寻路、吃方块，并受方块效果（加速/减速、生存时间增减、同屏方块数增减）影响。蛇死亡或场地耗尽后自动重开，右上角 HUD 持续显示统计信息。程序作为屏保运行时无人交互，全程由内置 AI 决策。

## 2. 技术选型

| 项 | 选型 | 说明 |
|---|---|---|
| 框架 | 原生 Win32 + C（WinAPI/GDI） | Windows 屏保的标准形态：单个自包含 `.scr`、原生处理 /s /c /p。此前 Electron 便携版与屏保协议不兼容（NSIS 桩把 `/c`/`/s` 当安装器开关，报 "error launching installer"），且体积下限 ~80MB 无法压到 10MB 内 |
| 渲染 | GDI（FillRect / TextOut），单元格 10px，**离屏双缓冲** | 屏保只画实心方块与文本，无 GPU 复杂度，CPU 占用极低；整帧先画到内存 DC 再一次 BitBlt 上屏，杜绝清屏/绘制过程暴露造成的黑屏闪 |
| 状态/统计持久化 | JSON 文件（`%APPDATA%\snake-screensaver\stats.json`） | 记录最大生存时间、成功/失败次数等跨会话数据，无需数据库 |
| 打包 | TinyCC 编译 `screensaver.c + game.c` 直接输出 `SnakeScreensaver.scr`（GUI 子系统） | 产物约 30KB、无运行时依赖；见第 3 节 |

## 3. Windows 屏幕保护程序（.scr）协议要求

Windows 把屏保当作带命令行参数的 exe（改名 .scr）调用，主进程必须处理三种参数：

| 参数 | 含义 | 行为 |
|---|---|---|
| `/s` | 正式运行 | 全屏、置顶、无边框窗口启动游戏；鼠标移动/按键/点击 → 退出屏保 |
| 无参数 | 测试 | 双击/右键「测试」→ **窗口化**运行（720×540，不监听退出、不隐藏光标），避免测试时直接弹全屏屏保 |
| `/c` | 配置 | 设置窗口：可调参数（速度/方块/缩场/临期）+ 7 种方块生成占比，保存到 `config.json` |
| `/p <hwnd>` | 小预览 | 在系统"屏幕保护程序设置"对话框的预览区嵌入渲染（MVP 阶段可只画静态帧或黑屏，标注 TODO） |

注意：
- 正式运行时必须 `fullscreen + alwaysOnTop + frame:false`，隐藏鼠标指针。
- 任意鼠标移动（阈值 > 10px，避免误触）、键盘、鼠标点击均退出进程。
- 打包：TinyCC 编译 `src/win/screensaver.c + src/win/game.c` 直接输出 `release/SnakeScreensaver.scr`（GUI 子系统、约 30KB），由用户放入 `C:\Windows\System32\` 或右键 .scr"测试"。

## 4. 核心规格

### 4.1 网格与蛇

- 全屏划分为正方形网格。**单元格边长 = 10px**（六号字 7.5pt 在 96 DPI 下的宽度），需按 DPI 缩放自适应（`devicePixelRatio`）。
- 蛇身粗细 = 1 个单元格，不可加宽。
- 蛇初始长度 3 节，出生位置：可操作区域中心附近，方向随机。
- 蛇移动为离散网格步进，基础速度定义为 **10 格/秒**（可调），效果 1/2 按百分比增减。
- 蛇身新增单元（吃方块后）颜色 = 被吃方块颜色。

### 4.2 方块系统

- 方块与蛇等宽（1 单元格），生成于可操作区域内**随机空格子**（不与蛇身、其他方块重叠）。
- 方块**恒为实色渲染，无闪烁、无呼吸、无临期预警**——不保留任何明暗变化效果（历次"闪烁/呼吸"反馈均指向观感抖动，已全部移除）。
- **每个方块存活时间初始 60 秒**；效果 3/4 动态增减后，新方块按当前全局生存时间生成，已在场的方块按剩余时间等比换算或保持不变（采用：**只影响后续生成**，已在场方块不回溯，实现简单）。
- 同时在场方块数有全局上限，初始值 **3**（待确认项①），效果 5/6 增减上限，下限 1。

#### 颜色、分数、效果对照表

| 编号 | 颜色（默认） | 分数（默认，待确认②） | 吃到后效果 |
|---|---|---|---|
| 1 | 红 #E5484D | 1 | 蛇速度 +1% |
| 2 | 绿 #46A758 | 2 | 蛇速度 -1% |
| 3 | 蓝 #3E63DD | 1 | 方块生存时间 +1 秒 |
| 4 | 黄 #FFB224 | 2 | 方块生存时间 -1 秒 |
| 5 | 紫 #8E4EC6 | 5 | 同屏方块数上限 +1 |
| 6 | 青 #00A2C7 | 1 | 同屏方块数上限 -1（下限 1） |
| 7 | 白 #FFFFFF | 5 | 随机触发上述 1~6 之一的效果 |

#### 生成概率（占比 10 : 9 : 4 : 4 : 3 : 3 : 1，总 34 份）

| 编号 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| 概率 | 29.4% | 26.5% | 11.8% | 11.8% | 8.8% | 8.8% | 2.9% |

分数设计原则：越稀有分数越高（默认值如上，可配置）。

### 4.3 寻路 AI（核心）

蛇头每步决策，目标选择算法：
1. **多目标 BFS**：从蛇头出发对网格做一次 BFS，得到到所有存活方块的最短路径长度 `d_i`（等权网格，BFS 即最短，无需 A*）。
2. **时间成本评估**：预计到达时间 `t_i = d_i / v`（v 为当前速度，格/秒）。方块剩余寿命 `r_i`。松弛量 `slack_i = r_i - t_i`。
3. **目标评分**：`score_i = 分数值 / (d_i + 1) × urgency`，其中 `urgency = slack_i < 5s 时权重提升`（临期方块优先抢救，避免触发缩场惩罚）。取 score 最高者为当前目标。
4. **安全性检查（防自杀）**：选定目标后，模拟蛇走到目标时的占位（蛇尾同时前移），对"到达后蛇头可达的空区域"做 flood fill；若可达空格数 < 蛇长（无逃生空间），放弃该目标改选次优，全部不安全时沿当前方向选择"能存活最久的方向"（贪心逃生：选择使 flood fill 面积最大的相邻格）。
5. **无路可走**：BFS 无可达方块（被场地边界/蛇身隔断）时，执行逃生模式（同上贪心 flood fill）。

### 4.4 场地缩小机制（惩罚）

- 触发条件：**任意 1 个方块超时未被吃掉** → 可操作区域立即缩小当前面积的 **1/10**。
- 缩小方式：从**外围**缩小且**位置不固定**——随机选择上/下/左/右其中一条边向内推进，推进深度 = `(当前面积 × 0.1) / 该边长度`（向上取整为格数）。
- **避开蛇身**：所选边的新边界若会覆盖蛇身或任何存活方块，则重新随机选边；四条边均被蛇身阻挡时，本次缩小**延迟**到某条边可行时立即执行（不豁免）。
- 被移除区域绘制为"围墙色"（深灰），蛇不可进入。
- 连锁判断：若本次缩小后，**剩余空地面积 < 当前可操作面积的 1/10**，即无法再执行下一次缩减 → 游戏结束（见 4.5）。

### 4.5 结束条件与重开

| 结束类型 | 判定 | 计入 |
|---|---|---|
| 成功 | 蛇身铺满整个可操作区域（无空格） | 成功次数 +1 |
| 失败-规则死亡 | 蛇头撞墙（可操作区域边界/围墙）或撞到自己身体 | 失败次数 +1 |
| 失败-场地耗尽 | 触发缩场但剩余空地不足以缩减 1/10 | 失败次数 +1 |
| 失败-时间耗尽 | 触发方块生命周期到期 | 失败次数 +1 |

（"成功=铺满屏幕"的解释为默认口径，待确认项③）

结束流程：
1. 结束画面定格 3 秒，显示本局摘要（得分、生存时长、结束原因）。
2. 若本局生存时间 > 历史最大生存时间，更新记录并持久化。
3. 全部统计（各色吃到个数、成功/失败次数、最大生存时间）写入 stats.json。
4. 重置为初始状态（初始场地、初始速度、初始方块参数），自动开始新一局。

### 4.6 HUD（右上角）

始终渲染在屏幕右上角，**面板 50% 半透明**（AlphaBlend，文字不透明保证可读），不遮挡主体太多画面。显示：

1. **7 种颜色方块各被吃掉的个数**（用对应颜色小方块 + 数字，7 行或紧凑网格）
2. **总得分**
3. **当前速度**（格/秒，考虑加速/减速效果）
4. **本局生存时间**（mm:ss）
5. **最大生存时间**（跨会话持久化）
6. **成功次数 / 失败次数**

统计口径：成功/失败次数与最大生存时间跨会话持久化；各色个数与总得分为**本局实时值**（是否累计跨局待确认项④，默认本局）。

## 5. 模块划分

```
src/
├── win/                  # 原生 C 实现（产品）
│   ├── screensaver.c     # Win32 壳：/s /c /p、退出监听、GDI 渲染、HUD、stats.json、config.json 读写 + /c 设置窗口
│   ├── game.c / game.h   # 纯游戏逻辑：状态机、网格、蛇、方块、寻路、效果、缩场、可调参数 Params
│   └── game_test.c       # 逻辑自检：AI 整局模拟 + 行为断言（控制台，无 GUI 可跑）
├── game/                 # TypeScript 逻辑参考实现（算法可执行规格，vitest 单测）
│   ├── engine.ts         # 游戏主循环、tick 调度、状态机
│   ├── grid.ts / snake.ts / block.ts / pathfinding.ts / effects.ts / arena.ts / stats.ts
└── config.ts             # 可调参数集中管理（TS 参考实现）
```

## 6. 关键实现注意事项

1. **游戏 tick 与渲染解耦**：逻辑固定步长（如 100ms 基准 ÷ 速度倍率），渲染按帧插值平滑移动；纯静态绘制，不做任何亮度/闪烁动画。
2. **速度倍率**：`speed = base × (1.01^加速次数) × (0.99^减速次数)`，用对数计数避免浮点连乘误差；不设上下限（按原需求，见待确认项⑤）。
3. **生存时间增减**：全局值 `blockLifetime`，初始 60，效果 3/4 ±1 秒；不设下限保护时需注意减到 0 的除零/瞬时超时问题（默认下限 1 秒，待确认项⑤）。
4. **缩场深度取整**：面积缩减按格数向上取整；格子级实现保证"避开蛇身"判定简单。
5. **DPI**：所有像素尺寸乘 `devicePixelRatio`，保证不同缩放比例下单元格物理尺寸一致。
6. **性能**：网格最大约 192×108（1080p/10px），BFS 与 flood fill 每步开销可忽略；每 tick 只在目标方块变化或每 N 步重算路径即可。
7. **屏保退出**：正式模式下监听 `mouse-move`（累计位移阈值）、`key-down`、`mouse-down` → `app.quit()`。
8. **多显示器**：MVP 只覆盖主屏，全屏窗口 `x:0,y:0`。
9. **无闪烁渲染**：整帧离屏双缓冲（`paint_frame`：清背景→画游戏→画 HUD 全在复用内存 DC，最后一次 `BitBlt`）；`WM_ERASEBKGND` 返回 TRUE 禁止系统用背景刷先擦黑；逻辑/渲染定时器间隔按 `EnumDisplaySettings` 刷新率对齐（60Hz→16ms）。结束定格→重开切换因整帧覆盖不出现黑屏闪。

## 7. 数据持久化

统计（`%APPDATA%\snake-screensaver\stats.json`）：

```json
{
  "maxSurvivalSeconds": 0,
  "successCount": 0,
  "failCount": 0,
  "colorCounts": [0,0,0,0,0,0,0],
  "totalScore": 0
}
```

写入时机：每局结束时。读取时机：启动时。

设置（`%APPDATA%\snake-screensaver\config.json`，由 `/c` 设置窗口读写）：

```json
{
  "cellSizePx": 10,
  "baseSpeed": 10,
  "initialBlockCap": 3,
  "blockLifetime": 60,
  "shrinkFraction": 0.1,
  "urgencyThreshold": 5,
  "urgencyFactor": 3,
  "endFreezeMs": 3000,
  "weights": [10,9,4,4,3,3,1]
}
```

- 启动时 `config_load()` 读取（缺失字段回退默认值），通过 `game_set_params()` 应用到 `Params`（game.c 顶部宏已改为运行时 `params` 字段）。
- 游戏参数默认值唯一来源为 `game.c` 的 `params_default()`（与 `src/config.ts` 一致），渲染参数（`cellSizePx`）由 screensaver.c 管理。

## 8. 待确认问题（实现前需与用户确认）

| # | 问题 | 当前默认口径 |
|---|---|---|
| ① | 初始同屏方块数上限 | 1 |
| ③ | "成功"的定义 | 铺满屏幕 = 成功；死亡/场地耗尽 = 失败 |
| ④ | 各色个数与总得分是否跨局累计 | 仅本局；最大生存时间与成功/失败次数跨会话累计 |
| ⑤ | 速度、生存时间是否设上下限 | 速度不设限；生存时间下限 1 秒 |
| ⑥ | 效果 3/4 是否影响在场方块 | 只影响后续生成的方块 |

## 9. 开发迭代计划

- **M1 骨架**：Win32 屏保壳（/s 全屏 + 鼠标键盘退出 + tcc 编译 .scr 脚本），GDI 网格 + 蛇基础移动。
- **M2 方块与觅食**：方块生成（占比抽样、60s 计时）、BFS 寻路 + 安全检查、吃方块增长/计分。
- **M3 效果系统**：7 种效果结算、速度倍率、生存时间、同屏数增减。
- **M4 缩场与结束**：超时缩场（随机边、避蛇身）、三种结束判定、结束画面与自动重开。
- **M5 HUD 与持久化**：右上角统计、stats.json、最大生存时间记录。
- **M6 打磨**：/c 配置窗口、/p 预览、多显示器、DPI 适配实测。

每个里程碑完成后在真机（改 .scr 放入 System32 → 设置屏保）实测验证，不实测不算完成。

## 10. 开发规范

- 所有可调参数集中在 `src/win/game.c` 的 `params_default()`（与 `src/config.ts` 一致），运行时可被 `/c` 配置覆盖，禁止魔法数字散落。
- 游戏逻辑（game.c / game.ts）不得依赖窗口/渲染 API：TS 参考实现用 vitest 单测；C 原生实现用 `game_test.c`（AI 整局模拟 + 行为断言）保证与参考实现行为一致。
- 每轮迭代由用户实测反馈 bug，修复后需复测通过才关闭问题。
- 需求变更：先改本文档，再改代码。
